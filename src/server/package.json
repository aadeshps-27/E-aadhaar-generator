{
  "name": "server",
  "version": "1.0.0",
  "description": "api for ecard generator",
  "main": "index.js",
  "scripts": {
    "start": "json-server --watch db.json --port 3000"
  },
  "author": "Aadeshps",
  "license": "ISC",
  "dependencies": {
    "json-server": "^0.17.3"
  }
}
